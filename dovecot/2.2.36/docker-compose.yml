version: '3'

services:

  dovecot:
    image:
      dovecot:2.2.36
    volumes:
      - /opt/usuarios:/opt/usuarios
      - /var/log/dovecot:/var/log/dovecot
      - /home/dominio:/home/dominios
    ports:
     - 110:110
     - 143:143
    networks:
      - localNetwork

networks:
  localNetwork:
